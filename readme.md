# Week-04, Day-07
Today's tasks was to create a mini backend project. I just integrated all the tasks from last few days. Login authentication, database creation, CRUD operations.
- The process remains the same, create server, make database, Write views, forms, models and run the server. Make the frontend page and deploy locally.
- The main challenge for today is to deploy the system in netlify so the website can be seen by anyone.
